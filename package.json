{
  "name": "fitment-calculator",
  "version": "1.0.0",
  "description": "Interactive wheel and tyre fitment calculator — static single-page app",
  "scripts": {
    "start": "node server.js",
    "caddy": "caddy run --config Caddyfile --watch",
    "check": "biome check --write .",
    "publish": "node build.js",
    "a11y": "playwright install chromium && node a11y.js"
  },
  "dependencies": {
    "express": "^4.22.2"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "@biomejs/biome": "2.4.16",
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0",
    "playwright": "^1.60.0",
    "terser": "^5.48.0"
  }
}
